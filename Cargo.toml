[workspace]
members = [
    "rox",
    "rox_diag",
    "rox_lex",
    "rox_parse",
    # Old version of interpreter
    "roxi",
]
resolver = "3"

[workspace.dependencies]
rox_diag = { path = "rox_diag" }
rox_lex = { path = "rox_lex" }
rox_parse = { path = "rox_parse" }
